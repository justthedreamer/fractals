export type CanvasProps = {
    width: number;
    height: number;
    clientWidth: number;
    clientHeight: number;
}

export type CanvasSettings = {
    width: number;
    height: number;
    color: string
}